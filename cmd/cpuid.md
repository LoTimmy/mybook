### 安裝 {#installing}

```console
shell> apt-get install cpuid
shell> cpuid
shell> cpuid | grep -i aes
```

```
      AES instruction                         = true
      AES instruction                         = true
```
